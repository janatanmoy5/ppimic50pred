services:
  - type: web
    name: ppim-ic50pred
    env: docker
    plan: free
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO
    branch: main
    dockerfilePath: Dockerfile
    healthCheckPath: /
    autoDeploy: true
